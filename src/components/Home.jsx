import Header from "./Header";

const Home = () => {
  return (
    <div>
      <button>
        <a href="">Create Quiz</a>
      </button>
      <button>
        <a href="">Continue Last Quiz</a>
      </button>
    </div>
  );
};

export default Home;
